interface:
  display_name: "Repo Synthesizer"
  short_description: "Write human-facing synthesis from repo evidence"
  default_prompt: "Use $repo-synthesizer to produce repo-understanding.json strictly from a verified harness package."
