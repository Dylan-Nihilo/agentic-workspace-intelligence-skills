interface:
  display_name: "Repo Fact Verifier"
  short_description: "Review node semantics or adjudicate Journeys"
  default_prompt: "Use $repo-fact-verifier for exactly one typed mode from $repo-understanding: independently review a bounded Stage 6 node-semantic catalog batch, or execute an adjudicate WorkItem v3. Reopen source evidence and never mutate catalogs or authoritative stores."
