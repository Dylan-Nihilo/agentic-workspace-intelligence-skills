# Product Map Retrieval Eval

Checks direct retrieval across the four v3 Product Maps. The eval measures deterministic map Hit@2 and MRR, verifies that all four maps share one projection key, and resolves every map Evidence reference against the authoritative semantic store or deterministic inventory.
