# Datasource Contract Eval

Preserves the non-repo-understanding regression gates for coding-pool normalization, audit export freshness, external runtime confirmation, CE shared ingest, and CE parse failure.

Run with `npm run eval:datasource`.
