# Trajectory Eval

Runs two independent v3 semantic ResearchContracts through dispatch, result detection, serial ingest, and Join. It verifies event ordering, v3 RunEvent schemas, SHA-256 hash continuity, and that projection/synthesis/completion gates cannot be skipped.
